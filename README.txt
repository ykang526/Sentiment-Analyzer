This is the final project for Ling406 Fall2020

Author: Yun Mo Kang
NetId: ykang19

Movie Review folder contains the jupyter notebook files for all the experiments conducted with different features.
Run it using jupyter noteboook. It will only run if the movie reviews folder (review_polarity) is in the same directory(Movie_Review)
The file is already in there so it should run fine.

Yelp folder contains the jupyter notebook files for all the experiments conducted with different features.
Run it using jupyter notebook. It will only run if the all_reivews.txt file is in the same directory (Yelp).
The file is already in there so it should run fine.